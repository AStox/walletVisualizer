setup:
  addons:
    - plan: heroku-redis
build:
  docker:
    server: server/Dockerfile
    celery: server/Dockerfile
    client: client/Dockerfile
  config:
    WEB3_INFURA_PROJECT_ID: ${WEB3_INFURA_PROJECT_ID}
    ETHERSCAN_API_KEY: ${ETHERSCAN_API_KEY}
# release:
#   command:
#     - ./deployment-tasks.sh
#   image: worker
run:
  server: ./start-gunicorn.sh
  celery: ./start-celery.sh
